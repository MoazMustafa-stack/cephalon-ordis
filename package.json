{
  "name": "cephalon-ordis",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @ordis/coordinator --filter @ordis/web dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "validate:skills": "node scripts/validate-skills.mjs",
    "audit:subscription-only": "node scripts/audit-subscription-only.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
